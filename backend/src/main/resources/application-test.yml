spring:
  datasource:
    url: jdbc:h2:mem:db_test
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    hibernate:
      format_sql: true